"""Standalone negative-scan capture tool (planned for v0.5.0).

Built and tested as its own module first, outside trichrome/main_window.py -
see the "Negative scan tool" section of CLAUDE.md. Run it directly for
testing via ``python3 -m trichrome.scan_tool``.
"""
